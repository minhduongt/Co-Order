// import { request } from "./utils";

// export const getAllPostsPaths = (params) =>
//   request
//     .get("/posts", {
//       ...params,
//     })
//     .then((res) => res.data.data.map(({ postId }) => postId));

// export const getAllPosts = (params) =>
//   request.get("/posts", { params }).then((res) => res.data);
